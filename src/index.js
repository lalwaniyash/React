import React from "react";
import ReactDOM from "react-dom";

//ReactDOM.render(WHAT TO Shared, WHERE TO SHOW IT)
ReactDOM.render(
  <div>
    <h1>Hello world!</h1>
    <p>This is a paragraph.</p>
  </div>,
  document.getElementById("root")
);
